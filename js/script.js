$(document).ready(function(){
   $('.sidenav').sidenav();
   $('.slider').slider({
      indicators: false,
      height: 500,
      transition: 700,
      interval: 10000
   });
   $('.materialboxed').materialbox();
});
